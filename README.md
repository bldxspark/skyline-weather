# Skyline Weather

A simple local React + Vite weather app.

## Prerequisites

- Node.js `>=22.13.0`

## Quick Start

```bash
npm install
npm run dev
```

## Commands

- `npm run dev` — start local development
- `npm run build` — build the production bundle
- `npm run preview` — preview the production build locally
  
